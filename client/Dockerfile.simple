# 使用本地已安装的JDK和Maven进行构建，避免Docker Hub依赖
FROM maven:3.9.4-eclipse-temurin-17

# 设置工作目录
WORKDIR /app

# 复制pom.xml和源代码
COPY ../server/pom.xml .
COPY ../server/src ./src

# 构建项目
RUN mvn clean package -DskipTests

# 暴露端口
EXPOSE 8080

# 启动应用
ENTRYPOINT ["java", "-jar", "target/erpcrm-server-1.0.0-SNAPSHOT.jar"]
